main:
    files:
      - { expand: true, flatten: true, src: ['app/*.html'], dest: 'dist/', filter: 'isFile' }
      - { expand: true, cwd: 'app/', src: ['assets/**/*'], dest: 'dist/', filter: 'isFile' }
