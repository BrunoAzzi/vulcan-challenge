default:
    - 'lint'
    - 'test'
    - 'build'
    - 'serve'

serve:
    - 'connect'
    - 'open'
    - 'watch'

lint:
    - 'jshint'

test:
    - 'karma'

build:
    - 'clean'
    - 'copy'
    - 'browserify'
