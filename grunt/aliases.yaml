default:
    - 'lint'
    - 'test'
    - 'build'
    - 'serve'

serve:
    - 'connect'
    - 'open'
    - 'watch'

lint:
    - 'jshint'

test:
    - 'karma'

'test:unit':
    - 'karma:unit'

'test:coverage':
    - 'karma:coverage'

build:
    - 'clean'
    - 'copy'
    - 'browserify'
